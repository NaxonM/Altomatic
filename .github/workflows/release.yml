name: Build and Release

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt pyinstaller

      - name: Build executable
        run: |
          pyinstaller Altomatic.spec

      - name: Package build output
        run: |
          $distDir = Join-Path $env:GITHUB_WORKSPACE "dist"
          if (-not (Test-Path $distDir)) {
            Write-Error "Build output directory not found: $distDir"
            exit 1
          }
          Push-Location $distDir
          Compress-Archive -Path "Altomatic\*" -DestinationPath "$env:GITHUB_WORKSPACE\Altomatic-${{ github.ref_name }}.zip"
          Pop-Location

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Altomatic-${{ github.ref_name }}
          path: Altomatic-${{ github.ref_name }}.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: Altomatic-${{ github.ref_name }}.zip
          tag_name: ${{ github.ref_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
